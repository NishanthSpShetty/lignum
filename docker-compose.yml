version: "3"

services:
        consul:
                image: consul
                ports:
                        - "8500:8500"
                        - "8600:8600"
                command:
                        - '--name=consul'
                        - 'consul agent -server -ui -node-server-1'
                        - '-bootstrap-expect=1 -client=0.0.0.0'

