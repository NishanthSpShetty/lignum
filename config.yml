# config file

server:
  host: "localhost"
  port: 8080
  service-key: "service/disqlogger/key/master"

consul:
  host: "localhost"
  port: 8500 
  sessionTTL: "15s"
  sessionRenewalTTL: "15s"

message:
  initial-size: 1000
